TЗ
Нужно подать на вход список слов (или фраз, или текстов), получить их векторное сходство между собой и на выходе отсортировать их по этому сходству по убыванию.

Т.е. это задача семантического поиска:

-   нужно получить embeddings для каждого слова / фразы (например, через OpenAI, BGE или e5)
-   затем вычислить попарное косинусное сходство
-   отсортировать пары по убыванию similarity и при необходимости сгруппировать наиболее близкие элементы
-   реализовать это на PHP, Python или Node.js

Пример списка слов:

seo
продвижение
сайт
маркетинг
купить
документация
история
блог
анализ
страница
программа
оптимизация
яндекс
google
скрейпинг
ссылки
pagerank
дубликаты
индексация
статья
парсинг
извлечение
контент

Вот нужно их отобразить по убыванию векторного сходства.12:50
Это интеграционная задача, принцип понятен, легче сделать на python там есть готовые решения(есть опыт обучения модели на python), на node.js будет чуть больше обвязки. Могу сделать это как тестовое, скрипт или эндпойнт13:31

Андрей
давайте + нужн а картинка, типа такой13:37
13:37
на чемписат ьне так важно

## VPS

SSH: `ssh root@157.22.252.36`, ключ `~/.ssh/id_ed25519`. Без пароля, без Docker.
Если не заходит — проверить что юзер `root`, не `evgenmagger`.

| Ресурс | Значение |
|---|---|
| RAM | 1.9 GB (свободно ~1.3 GB) |
| CPU | 1 vCPU |
| Disk | 30 GB (свободно 11 GB) |
| OS | Ubuntu 24.04 |
| Python | 3.12.3 |
| pip | не установлен |
| Docker | не установлен |

Docker не ставили, потому что на 1.9 GB RAM он съест лишние 200-300 MB на демона, а модель и так лёгкая (130 MB). Проще запускать процессы напрямую.

**План**: сначала локальный PoC (Python-скрипт + Node.js рядом), без systemd/докера. Если покатит и будет расширяться — выпилить всё с VPS и пересобрать через Docker (Dockerfile + docker-compose).
